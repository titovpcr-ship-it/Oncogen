__pycache__/
*.pyc
candidates_*.csv
smoke_test_candidates.csv
smoke_test_decisions_log.csv
.env
.pw-profile/
.mv_sitemap_cache/
vinyl.db
vinyl.db-journal
vinyl.db-wal
# -shm появился вместе с режимом WAL: это разделяемый индекс сессий
# sqlite, спутник базы, а не данные. Без него правило про -wal
# неполно — файл всплыл в первом же коммите после включения WAL.
vinyl.db-shm

# Логи прогонов: файл растёт во время работы и делает дерево грязным
# на каждой проверке. Результат прогона живёт в vinyl.db, не в логе.
logs/
